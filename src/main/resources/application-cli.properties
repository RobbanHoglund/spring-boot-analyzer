# CLI mode: suppress Spring Boot banner and all non-essential logging so that
# only the analysis report is written to stdout. Errors in the analyzer itself
# are still printed to stderr via the root logger at WARN level.
spring.main.banner-mode=off
logging.level.root=WARN
logging.level.com.robbanhoglund=WARN
