{
  "name": "spring-boot-analyzer-frontend",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "overrides": {
    "esbuild": "^0.28.1"
  },
  "devDependencies": {
    "jsdom": "^26.1.0",
    "playwright": "^1.61.0",
    "typescript": "^6.0.3",
    "vite": "^8.0.10",
    "vitest": "^3.2.4"
  }
}
